<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ben.mapper.BlogMapper">
    <select id="getBlogTypes" resultType="java.util.Map">
        select distinct type, count(id) as count from blog where published = 1 and type is not null group by type;
    </select>

    <select id="getLatestBlogId" resultType="Long">
        select id from blog order by published_time desc limit 1;
    </select>
</mapper>